services:
  builder:
    build: .
    volumes:
      - .:/app
    container_name: stremio_addon_builder

  php-editor:
    image: php:8.2-cli
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8080:8080"
    command: php -S 0.0.0.0:8080 -t . editor.php